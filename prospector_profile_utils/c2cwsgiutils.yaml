pylint:
  disable:
    - missing-timeout # Missing timeout argument for method 'requests.get' can cause your program to hang indefinitely. Done by c2cwsgiutils.

ruff:
  disable:
    - S113 # Probable use of `requests` call without timeout. Done by c2cwsgiutils.

bandit:
  disable:
    - B113 # Requests call without timeout. Done by c2cwsgiutils.
